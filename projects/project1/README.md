# EECS 662 - Project 0
#### Author: Jace Kline

### Build Instructions
Run the command `make` or `make test` to build the tester executable. The test output will be stored in the 'test-results.txt' file.

### Clean Instructions
Run the command `make clean` to clean all executables, object files, linker files, and the test output file.

